import  { Game } from "../Game.mjs"

describe('Game', () => {
    test('Should start a game', ()=>{
        const game = new Game()
        const output = 
        expect(cell.get_status()).toBe(output)
    })
})